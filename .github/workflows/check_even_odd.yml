name: check even or odd

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: check lines
        run: |
          count=`cat READMD.md | wc -l`
