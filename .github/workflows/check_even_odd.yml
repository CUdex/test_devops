name: check even or odd

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: check lines
        run: cat READMD.md | wc -l

      - name: check dir
        run: ls -al

      - name: start check
        run: bash check_even_odd.sh
